package com.gestormc.gestor.util

// The modern Task API

/**
 * The [Tasks] object contains references to all of the tasks.
 *
 * Right here, it doesn't store anything, because the references are inserted via extension properties.
 */
object Tasks
